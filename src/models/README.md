# Hazard Modelling

This module generates hazard representations of the terrain.

Modules:

- `crater_predictor.py`
  Generates crater hazard maps using catalogue data or ML models.

- `hazard_assessor.py`
  Combines terrain features and crater hazards into a unified hazard map.