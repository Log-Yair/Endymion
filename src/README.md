# Core System Modules

The `src` directory contains the core modules that implement the Endymion
navigation pipeline.

Each module corresponds to a specific stage of the navigation system.

Pipeline flow:

Data → Feature Extraction → Hazard Modelling → Path Planning → Evaluation