# Navigation Planning

The planning module generates safe traversal paths across the terrain.

Current implementation:

- `pathfinder.py`

Implements a weighted A* search algorithm using hazard cost maps.